PyQt5>=5.15
pyqtgraph>=0.13.3
PyOpenGL>=3.1.6
numpy>=1.24
scipy>=1.10

# --- Modulo acustico: mallado boundary-fitted + import CAD ---
# Obligatorio: gmsh (kernel OpenCASCADE) para STEP/IGES/BREP y tetra mesh.
# El usuario puede no usarlo (combo "Voxel" en panel Acustica) pero la
# instalacion debe traerlo para tener TODOS los formatos CAD disponibles.
gmsh>=4.13
trimesh>=4.0
# Backend de boolean de mallas de trimesh (sala - columna/obstaculo -> superficie
# con tunel para mallado gmsh boundary-fitted; a futuro, resta de muebles en alta
# frecuencia). Es geometria (no fisica): mismo alcance que trimesh/gmsh en D0.
manifold3d>=2.5
# Remesh isotropico (MeshLab) para superficies CURVAS importadas o CAD sucio
# (EASE) donde la reparametrizacion de gmsh falla ("overlapping facets"). Se
# remalla uniforme -> gmsh malla el volumen con esa frontera DISCRETA fija
# (boundary-fitted). Es geometria/IO (no fisica): mismo alcance D0 que gmsh.
# OPCIONAL: si falta, el router degrada a voxel (nunca deja sin malla).
pymeshlab>=2023.12
matplotlib>=3.7  # graficos de FRF y RT60
